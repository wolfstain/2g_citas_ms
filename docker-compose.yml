version: '2'

services:
  citas-ms:
    build: .     
    ports:
      - "4002:3300"
    depends_on:
      - citas-bd
  citas-bd:
    image: mongo
    ports:
      - "27018:27017"
    volumes:
      - "mongodata:/data/2g_citas_bd"
    networks:
      - citas-bd
  
volumes:
   mongodata:

networks:
  citas-bd:
